## [2026-03-22] shadcn init file concatenation corruption | What broke: `components.json`, `app/globals.css`, `components/ui/button.tsx`, and `lib/utils.ts` were concatenated with duplicate content and caused build failures. | Root cause: interrupted interactive `npx shadcn@latest init` writes in the persistent terminal appended partial template output. | Fix: manually cleaned corrupted files, restored valid shadcn-compatible config/components, and revalidated with `npm run build`. | File(s): components.json, app/globals.css, components/ui/button.tsx, lib/utils.ts, app/layout.tsx
## [2026-03-22] quick actions button prop mismatch | What broke: dashboard build failed with a TypeScript error because `asChild` was passed to `Button`, but the local `Button` component does not support that prop. | Root cause: used a shadcn pattern variant (`asChild`) without local component support. | Fix: replaced `Button asChild` usage with `Link` + `buttonVariants` styling. | File(s): components/dashboard/QuickActions.tsx
## [2026-03-22] supabase env missing caused prerender crashes | What broke: pages that instantiate Supabase clients during SSR/prerender threw `@supabase/ssr` missing URL/API key errors on fresh environments. | Root cause: direct non-null env usage in Supabase client/server helpers and middleware assumed configured env vars. | Fix: added centralized Supabase env resolver with safe fallbacks and middleware short-circuit when env is not configured. | File(s): lib/supabase/env.ts, lib/supabase/client.ts, lib/supabase/server.ts, middleware.ts
## [2026-03-23] dashboard route auth gap blocked secure proposal sharing | What broke: dashboard-style routes under `/proposals`, `/follow-ups`, `/tasks`, and similar prefixes were reachable without login because middleware only redirected `/dashboard/*`; task-008 also needed exactly one public exception for proposal share pages. | Root cause: matcher included multiple protected prefixes, but the middleware guard checked only `pathname.startsWith("/dashboard")`. | Fix: updated middleware to protect all dashboard app prefixes while explicitly exempting only `/proposals/view/:token`, and implemented token-based public proposal APIs using the service-role client. | File(s): middleware.ts, lib/supabase/admin.ts, app/api/proposals/view/[token]/route.ts, app/api/proposals/[id]/accept/route.ts
## [2026-03-23] invoice pdf utility TSX parse error and DOM shadowing | What broke: invoice PDF utility initially used JSX in a `.ts` file and invoice detail download shadowed `document`, causing parse/runtime risks. | Root cause: wrong file extension for JSX helper and local variable naming collision with the DOM global. | Fix: moved PDF helper to `lib/utils/pdf.tsx`, deleted `lib/utils/pdf.ts`, and renamed local `document` variable to `invoiceDocument` while using `window.document.createElement`. | File(s): lib/utils/pdf.tsx, app/(dashboard)/invoices/[id]/page.tsx
## [2026-03-23] shared button variant gap blocked invoice build | What broke: the invoice creation page used `variant="ghost"`, but the shared `Button` component only exposed `default` and `outline`, causing `tsc` and `next build` to fail. | Root cause: local shadcn-style button variants fell out of sync with actual component usage across the app. | Fix: added the missing `ghost` variant to the shared button component so existing callers type-check and render consistently. | File(s): components/ui/button.tsx
## [2026-03-23] recharts formatter typing on income chart | What broke: task-011 introduced strict TypeScript errors in Recharts axis/tooltip formatter callbacks. | Root cause: formatter callback parameters were typed as `number`, but Recharts expects broader `ValueType` unions including undefined. | Fix: normalized formatter inputs with `Number(value ?? 0)` in both Y-axis and tooltip callbacks. | File(s): components/dashboard/IncomeHistoryChart.tsx
## [2026-03-23] public proposal acceptance route exceeded allowed unauthenticated surface | What broke: proposal acceptance was possible without authentication via share token, which violated task-014's public-route constraint. | Root cause: `app/api/proposals/[id]/accept` implemented a service-role fallback branch accepting `{ share_token }` for anonymous users. | Fix: removed anonymous acceptance path and service-role usage from the accept route, enforced authenticated ownership checks, and updated public proposal UI to handle 401 with a sign-in message; added explicit Zod shape validation in settings PUT as additional input-hardening. | File(s): app/api/proposals/[id]/accept/route.ts, components/proposals/PublicProposalView.tsx, app/api/settings/route.ts
